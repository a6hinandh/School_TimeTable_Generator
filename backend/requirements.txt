fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
ortools==9.8.3296
python-multipart==0.0.6



# Install these:

# python -m pip install "pymongo[srv]" (run this command)
# (pip install the following):
# pymongo 
# python-dotenv

# To run the server, use this command:
# uvicorn server:app --reload
